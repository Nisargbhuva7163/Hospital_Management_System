<%= form_with(model: [@organization, booking_window], local: true, class: "w-100") do |f| %>
  <div class="container mt-4">
    <div class="card shadow-sm rounded-4">
      <div class="card-header bg-primary text-white rounded-top-4">
        <h5 class="mb-0">🕒 Add Booking Window</h5>
      </div>
      <div class="card-body">
        <% if booking_window.errors.any? %>
          <div class="alert alert-danger rounded-3">
            <h5><%= pluralize(booking_window.errors.count, "error") %> prohibited this booking window from being saved:</h5>
            <ul class="mb-0">
              <% booking_window.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
              <% end %>
            </ul>
          </div>
        <% end %>

        <div class="row g-3">
          <div class="col-md-6">
            <%= f.label :start_time, class: "form-label" %>
            <%= f.time_select :start_time, { default: Time.current, minute_step: 5 }, { class: "form-select rounded-3" } %>
          </div>
          <div class="col-md-6">
            <%= f.label :end_time, class: "form-label" %>
            <%= f.time_select :end_time, { default: Time.current + 1.hour, minute_step: 5 }, { class: "form-select rounded-3" } %>
          </div>
        </div>

        <div class="mt-4 text-end">
          <%= f.submit "💾 Save", class: "btn btn-success px-4 py-2 rounded-3" %>
        </div>
      </div>
    </div>
  </div>
<% end %>
